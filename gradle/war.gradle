apply plugin: "war"

bootWar {
    mainClass = "com.bits.opensociety.VisitorServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
